import 'package:flutter/material.dart';

class CatadorMapPage extends StatelessWidget {
  const CatadorMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
